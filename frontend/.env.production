# Backend the production build talks to (live prod data on Vercel).
# Used at build time by Vite as import.meta.env.VITE_API_BASE_URL.
# No trailing slash — the code appends paths like `${API_BASE}/graphql`.
VITE_API_BASE_URL=https://safety-dashboard-phi.vercel.app
